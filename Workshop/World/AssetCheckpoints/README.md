# Private asset checkpoints

This folder contains lightweight, source-controlled indexes for large game-asset checkpoints.

The binary authoring packages remain in private GitHub Releases rather than Git history. Each index records the exact release tag, hashes, validation scope, and deliberately open design decisions needed to recover or continue the asset without inflating what has been proven.

